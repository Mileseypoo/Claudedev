import { describe, it } from 'vitest'

describe('useDeepgramStream', () => {
  it.todo('fires POST /api/session/chunk on each is_final Deepgram transcript event')
  it.todo('does not fire POST for non-final results')
})
