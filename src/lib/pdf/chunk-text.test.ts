import { describe, it } from 'vitest'

describe('chunkText', () => {
  it.todo('text of length 5000 with default chunkSize=2048 overlap=400 produces correct chunk count')
  it.todo('each chunk is at most chunkSize characters')
  it.todo('overlap: text at position chunkSize-overlap appears in both chunk 0 and chunk 1')
})
