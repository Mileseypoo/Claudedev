import { describe, it } from 'vitest'

describe('parseCsv', () => {
  it.todo('returns all valid rows when CSV has all required columns with valid data')
  it.todo('returns per-column error messages when required columns are missing')
  it.todo('returns row-level errors for rows with invalid enum values or type mismatches')
})
