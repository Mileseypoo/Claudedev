---
phase: 01-foundation
plan: "03"
type: execute
wave: 1
depends_on:
  - "01-01"
files_modified:
  - src/app/manifest.ts
  - src/app/layout.tsx
  - src/app/page.tsx
  - src/app/(session)/active/page.tsx
  - src/app/(session)/recovery/page.tsx
  - src/app/globals.css
  - public/sw.js
  - public/icon-192.png
  - public/icon-512.png
autonomous: true
requirements:
  - PLAT-01

must_haves:
  truths:
    - "Opening the app at / renders a Start Session screen (or recovery screen if localStorage key exists)"
    - "manifest.ts returns display: standalone with correct name, icons, and theme colors"
    - "The app is installable as a PWA on iOS and Android (manifest + service worker present)"
    - "The active session route /active exists with the branded dark layout"
    - "The recovery screen route /recovery exists"
  artifacts:
    - path: "src/app/manifest.ts"
      provides: "PWA manifest via Next.js built-in manifest route"
      contains: "display: 'standalone'"
    - path: "src/app/layout.tsx"
      provides: "Root layout with brand theme CSS variables; dark background; viewport meta for mobile"
    - path: "src/app/page.tsx"
      provides: "Home page: checks localStorage for recovery key and redirects to /recovery, else shows start-session placeholder"
    - path: "src/app/(session)/active/page.tsx"
      provides: "Active session page shell (dark, full-screen, placeholder components)"
    - path: "src/app/(session)/recovery/page.tsx"
      provides: "Recovery screen shell with Resume/Start Fresh button placeholders"
    - path: "public/sw.js"
      provides: "Minimal service worker for PWA installability (install + fetch events)"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/globals.css"
      via: "import globals.css; CSS vars --brand-color, --brand-bg used throughout"
      pattern: "import.*globals.css"
    - from: "src/app/page.tsx"
      to: "dictator_session_recovery localStorage key"
      via: "useEffect on mount checks localStorage"
      pattern: "SESSION_RECOVERY_KEY"
---

<objective>
Build the PWA shell: manifest, root layout with brand theming, page routing structure, and a minimal service worker. This plan establishes all the routes and the installable PWA foundation that UI components slot into in later plans.

Purpose: PLAT-01 requires the app to install as a PWA on iOS and Android. The manifest and service worker must exist before any device testing is possible.
Output: Installable PWA shell with four routes (/, /active, /recovery), dark brand theme, and placeholder page components.
</objective>

<execution_context>
@C:/Users/Miles/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Miles/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PWA manifest and root layout</name>
  <files>
    src/app/manifest.ts,
    src/app/layout.tsx,
    src/app/globals.css
  </files>
  <behavior>
    - manifest() returns object where `display === 'standalone'`
    - manifest() returns object with `name === 'Dictator — Sales Copilot'`
    - manifest() returns object with `short_name === 'Dictator'`
    - manifest() includes icons array with at least a 192x192 and 512x512 entry
    - manifest() returns `start_url === '/'`
    - manifest() returns `background_color === '#0f0f1a'`
    - manifest() returns `theme_color === '#0f0f1a'`
  </behavior>
  <action>
    Create `src/app/manifest.ts` — exactly as shown in the research file Code Examples section (PWA Manifest). Do not deviate from the specified fields. `display: 'standalone'`, `orientation: 'portrait'`, dark background `#0f0f1a`.

    Create `src/app/globals.css`:
    - CSS custom properties on `:root`: `--brand-bg: #0f0f1a`, `--brand-color: #7c3aed` (vibrant purple — overridable per tenant in v2), `--brand-surface: #1a1a2e`, `--text-primary: #f8f8f8`, `--text-muted: #94a3b8`
    - Reset: `*, *::before, *::after { box-sizing: border-box }`, `html, body { height: 100% }`, `body { margin: 0 }`
    - Mobile optimisation: `html { font-size: 16px; -webkit-text-size-adjust: 100% }`, `button { touch-action: manipulation }` (prevents double-tap zoom on buttons)

    Create `src/app/layout.tsx`:
    - Import globals.css
    - `<html lang="en" className="h-full">`, `<body>` with `className="h-full bg-[var(--brand-bg)] text-[var(--text-primary)] antialiased"`
    - Viewport meta in `export const metadata`: `viewport: { width: 'device-width', initialScale: 1, maximumScale: 1 }` (prevents zoom disrupting the session UI)
    - Register service worker: inline `<script>` in layout that runs `navigator.serviceWorker?.register('/sw.js')` — use `dangerouslySetInnerHTML` or a small `'use client'` component `ServiceWorkerRegistrar` if preferred.

    Create placeholder icon files: use a simple 1x1 PNG or copy from a public domain source. The icons must exist at `public/icon-192.png` and `public/icon-512.png` — even if placeholder — so the PWA manifest validation passes. Generate with:
    ```bash
    # Quick placeholder — 1px dark PNG (not production quality, acceptable for POC)
    # The executor should create minimal valid PNG files here
    ```
    If no image generation is available, create a minimal valid PNG with Node.js Buffer or copy a tiny valid PNG binary. The files must be non-empty.
  </action>
  <verify>
    <automated>cd c:/ClaudeDev/dictator && npx vitest run src/app/manifest.test.ts --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>manifest.ts exports the correct PWA manifest. `npx vitest run src/app/manifest.test.ts` exits 0. globals.css and layout.tsx exist with mobile-optimised body class and service worker registration. Icon files exist at public/icon-192.png and public/icon-512.png.</done>
</task>

<task type="auto">
  <name>Task 2: Page routing and service worker</name>
  <files>
    src/app/page.tsx,
    src/app/(session)/active/page.tsx,
    src/app/(session)/recovery/page.tsx,
    public/sw.js
  </files>
  <action>
    Create `public/sw.js` — minimal service worker sufficient for PWA installability:
    ```javascript
    // Minimal service worker — enables PWA install
    // Phase 1: no offline caching; just install + fetch passthrough
    const CACHE_VERSION = 'v1'

    self.addEventListener('install', (event) => {
      self.skipWaiting()
    })

    self.addEventListener('activate', (event) => {
      event.waitUntil(clients.claim())
    })

    self.addEventListener('fetch', (event) => {
      // Pass through all requests — no caching in Phase 1
      event.respondWith(fetch(event.request))
    })
    ```

    Create `src/app/page.tsx` (home / entry screen):
    - `'use client'` — needs localStorage access
    - Import `SESSION_RECOVERY_KEY` from `@/lib/constants`
    - `useEffect` on mount: check `localStorage.getItem(SESSION_RECOVERY_KEY)` — if truthy, call `router.replace('/recovery')` using `useRouter` from `next/navigation`
    - While checking (brief), render `null` or a blank dark screen to avoid flash
    - Default render (no recovery): a full-height dark screen with the app name "Dictator" centered, and a `<button>` labeled "Start Session" (onClick placeholder — wired in Plan 05). Button should use Tailwind: large, pill-shaped, brand-colored, thumb-sized tap target (`min-h-[56px] px-10 rounded-full`).

    Create `src/app/(session)/active/page.tsx`:
    - `'use client'`
    - Placeholder shell: full-height dark background `bg-[var(--brand-bg)]`
    - Three placeholder regions using Tailwind `flex flex-col h-full`:
      - Top area (grows): `{/* Cards appear here in Phase 3 */}` comment
      - Center: mic indicator placeholder `<div className="flex-1 flex items-center justify-center">` with a comment `{/* MicIndicator + SessionTimer */}`
      - Bottom (fixed): `<div className="pb-8 px-6">` with a comment `{/* SessionControls (Pause + End) */}` and two placeholder buttons
    - Export `default function ActiveSessionPage()`

    Create `src/app/(session)/recovery/page.tsx`:
    - `'use client'`
    - Full-height dark screen with a recovery prompt:
      - Heading: "Session in progress"
      - Subtext: "Your last session was interrupted."
      - Two large pill buttons: "Resume session" (brand-colored) and "Start fresh" (outlined/muted)
      - Both buttons are onClick placeholders for now — wired in Plan 06
    - Export `default function RecoveryPage()`
  </action>
  <verify>
    <automated>cd c:/ClaudeDev/dictator && npx vitest run src/app/page.test.tsx --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>
    Four files created: page.tsx, active/page.tsx, recovery/page.tsx, public/sw.js.
    Home page checks localStorage for recovery key and navigates to /recovery if present.
    `npx vitest run src/app/page.test.tsx` exits 0.
    `npx next build` completes without errors (run as a sanity check after creating all files).
  </done>
</task>

</tasks>

<verification>
```
cd c:/ClaudeDev/dictator && npx vitest run src/app/manifest.test.ts src/app/page.test.tsx --reporter=verbose
```
Expected: exit 0, both files show todo stubs (no failures).
</verification>

<success_criteria>
- manifest.ts returns correct PWA fields including display: standalone
- layout.tsx imports globals.css and registers service worker
- public/sw.js exists as minimal passthrough service worker
- / page checks localStorage and routes to /recovery if session recovery key present
- /(session)/active/page.tsx exists as dark shell with three placeholder regions
- /(session)/recovery/page.tsx exists with Resume/Start Fresh placeholders
- `npx next build` completes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
