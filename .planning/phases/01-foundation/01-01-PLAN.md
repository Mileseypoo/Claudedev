---
phase: 01-foundation
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - src/test/setup.ts
  - src/hooks/useSessionLifecycle.test.ts
  - src/hooks/useAudioCapture.test.ts
  - src/hooks/useDeepgramStream.test.ts
  - src/components/session/SessionControls.test.tsx
  - src/components/session/MicIndicator.test.tsx
  - src/app/page.test.tsx
  - src/app/manifest.test.ts
  - src/app/api/session/end/route.test.ts
  - src/app/api/session/start/route.test.ts
  - src/db/rls.test.ts
autonomous: true
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
  - SESS-04
  - SESS-05
  - SESS-06
  - PLAT-01
  - PLAT-02

must_haves:
  truths:
    - "Running `npx vitest run --reporter=verbose` exits with code 0 (all stubs pass)"
    - "Every requirement mapping in VALIDATION.md has a corresponding test stub file"
    - "Mocks for MediaRecorder, WakeLock, Supabase client, and getUserMedia are in shared setup"
  artifacts:
    - path: "vitest.config.ts"
      provides: "Vitest configuration with jsdom environment and React plugin"
    - path: "src/test/setup.ts"
      provides: "Shared mocks: MediaRecorder, WakeLock navigator API, Supabase client, getUserMedia"
    - path: "src/hooks/useSessionLifecycle.test.ts"
      provides: "Stub tests for SESS-01 (start), SESS-05 (pause/resume), SESS-06 (localStorage key)"
    - path: "src/hooks/useAudioCapture.test.ts"
      provides: "Stub tests for SESS-01 (getUserMedia timing), SESS-04 (permission errors)"
    - path: "src/hooks/useDeepgramStream.test.ts"
      provides: "Stub test for SESS-06 (chunk persistence on is_final events)"
    - path: "src/components/session/SessionControls.test.tsx"
      provides: "Stub test for SESS-02 (end confirmation tap)"
    - path: "src/components/session/MicIndicator.test.tsx"
      provides: "Stub test for SESS-03 (aria-label present during active recording)"
    - path: "src/app/page.test.tsx"
      provides: "Stub test for SESS-06 (recovery screen renders when localStorage key exists)"
    - path: "src/app/manifest.test.ts"
      provides: "Stub tests for PLAT-01 (manifest fields, display: standalone)"
    - path: "src/app/api/session/end/route.test.ts"
      provides: "Stub test for SESS-02 server-side (session record set to ended)"
    - path: "src/app/api/session/start/route.test.ts"
      provides: "Stub test for PLAT-02 (session created with DEFAULT_TENANT_ID)"
    - path: "src/db/rls.test.ts"
      provides: "Stub test for PLAT-02 (RLS policy blocks cross-tenant access)"
  key_links:
    - from: "vitest.config.ts"
      to: "src/test/setup.ts"
      via: "setupFiles option"
      pattern: "setupFiles.*setup"
    - from: "src/test/setup.ts"
      to: "all test files"
      via: "global mocks applied before every test"
      pattern: "vi\\.stubGlobal|vi\\.mock"
---

<objective>
Bootstrap the test infrastructure for Phase 1. Create the Vitest config, shared test setup with mocks, and stub test files for all 16 requirement-to-test mappings defined in VALIDATION.md. All stubs start as failing `todo` placeholders — they turn green only when the corresponding implementation is in place.

Purpose: Nyquist compliance requires automated verify commands on every task. All Wave 1+ tasks reference these test files. Without them, execution cannot proceed.
Output: Working Vitest install, all stub test files, shared mocks for browser APIs that don't exist in jsdom (MediaRecorder, WakeLock, getUserMedia).
</objective>

<execution_context>
@C:/Users/Miles/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Miles/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-VALIDATION.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install test framework and create Vitest config</name>
  <files>package.json, vitest.config.ts, src/test/setup.ts</files>
  <action>
    This is a greenfield Next.js project. First initialise the project if package.json does not exist:
    ```
    npx create-next-app@latest . --typescript --tailwind --eslint --app --src-dir --import-alias "@/*" --no-git
    ```
    Then install test dependencies:
    ```
    npm install -D vitest @vitejs/plugin-react jsdom @testing-library/react @testing-library/user-event @vitest/coverage-v8
    npm install -D @playwright/test
    ```

    Create `vitest.config.ts` at project root:
    - environment: jsdom
    - plugins: [react()]
    - setupFiles: ['./src/test/setup.ts']
    - include: ['src/**/*.test.ts', 'src/**/*.test.tsx']
    - exclude: ['node_modules', '.next', 'playwright']
    - Add `"test": "vitest run --reporter=verbose"` to package.json scripts.

    Create `src/test/setup.ts` with global mocks required by all test files:
    - **MediaRecorder mock**: stub class with `start()`, `pause()`, `resume()`, `stop()`, `ondataavailable` handler, `state` property ('inactive'|'recording'|'paused'), and `isTypeSupported` static returning true.
    - **WakeLock mock**: stub `navigator.wakeLock.request()` that resolves with a sentinel object `{ release: vi.fn(), addEventListener: vi.fn() }`.
    - **getUserMedia mock**: stub `navigator.mediaDevices.getUserMedia` that resolves with a mock MediaStream (tracks array with one audio track that has a `stop()` fn).
    - **Supabase mock**: export `mockSupabaseClient` with chainable `.from()`, `.upsert()`, `.insert()`, `.select()`, `.eq()`, `.update()` — all returning `{ data: null, error: null }` by default. Used by individual tests via `vi.mock('@/lib/supabase/client')`.
    - Import `@testing-library/jest-dom` matchers via `import '@testing-library/jest-dom'` (install the package too: `npm install -D @testing-library/jest-dom`).
    - Use `vi.stubGlobal` for MediaRecorder and WakeLock so they're available globally in jsdom.
  </action>
  <verify>
    <automated>cd c:/ClaudeDev/dictator && npx vitest run --reporter=verbose 2>&1 | tail -5</automated>
  </verify>
  <done>Vitest installs cleanly. `npm test` runs without error (zero test files found is acceptable at this point — no crash). vitest.config.ts and src/test/setup.ts exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create all stub test files (Wave 0 stubs)</name>
  <files>
    src/hooks/useSessionLifecycle.test.ts,
    src/hooks/useAudioCapture.test.ts,
    src/hooks/useDeepgramStream.test.ts,
    src/components/session/SessionControls.test.tsx,
    src/components/session/MicIndicator.test.tsx,
    src/app/page.test.tsx,
    src/app/manifest.test.ts,
    src/app/api/session/end/route.test.ts,
    src/app/api/session/start/route.test.ts,
    src/db/rls.test.ts
  </files>
  <behavior>
    Each stub file contains `describe` + `it.todo` blocks. The `it.todo` entries do NOT fail — they are skipped by Vitest and show as "todo". This keeps the suite green while signalling what must be implemented.

    - src/hooks/useSessionLifecycle.test.ts:
      - it.todo: "consent confirm transitions state to active and calls getUserMedia"
      - it.todo: "pause transitions state to paused and calls MediaRecorder.pause()"
      - it.todo: "resume transitions state to active and calls MediaRecorder.resume()"
      - it.todo: "localStorage key dictator_session_recovery written on session start"
      - it.todo: "localStorage key cleared on clean session end"

    - src/hooks/useAudioCapture.test.ts:
      - it.todo: "getUserMedia called inside consent confirm handler, not on mount"
      - it.todo: "NotAllowedError returns error: not_allowed"
      - it.todo: "NotFoundError returns error: not_found"
      - it.todo: "getSupportedMimeType returns first supported type"

    - src/hooks/useDeepgramStream.test.ts:
      - it.todo: "fires POST /api/session/chunk on each is_final Deepgram transcript event"
      - it.todo: "does not fire POST for non-final results"

    - src/components/session/SessionControls.test.tsx:
      - it.todo: "End button requires a second tap to confirm before onEnd is called"
      - it.todo: "Pause button calls onPause immediately without confirmation"

    - src/components/session/MicIndicator.test.tsx:
      - it.todo: "renders with aria-label 'Recording active' when isRecording is true"
      - it.todo: "does not render pulsing class when isRecording is false"

    - src/app/page.test.tsx:
      - it.todo: "renders recovery screen when dictator_session_recovery exists in localStorage"
      - it.todo: "renders start screen when no recovery key in localStorage"

    - src/app/manifest.test.ts:
      - it.todo: "manifest returns display: standalone"
      - it.todo: "manifest includes name, short_name, start_url, background_color, theme_color, icons"

    - src/app/api/session/end/route.test.ts:
      - it.todo: "POST /api/session/end updates session status to ended in Supabase"

    - src/app/api/session/start/route.test.ts:
      - it.todo: "POST /api/session/start creates session record with DEFAULT_TENANT_ID"

    - src/db/rls.test.ts:
      - it.todo: "Supabase query without tenant context returns empty result (RLS blocks)"
      - it.todo: "Supabase query with correct tenant_id returns session records"
  </behavior>
  <action>
    Create each file exactly as described in the behavior block above. Use `describe('[hook/component name]', () => { it.todo(...) })` structure.

    Do NOT import from implementation files yet — the implementations don't exist. Each file imports only from vitest (`import { describe, it } from 'vitest'`).

    Exception: `src/app/manifest.test.ts` can import the manifest function once `src/app/manifest.ts` exists (it won't yet — keep it as `it.todo`).
  </action>
  <verify>
    <automated>cd c:/ClaudeDev/dictator && npx vitest run --reporter=verbose 2>&1 | grep -E "passed|failed|todo|error"</automated>
  </verify>
  <done>All 10 test files exist. `npx vitest run` exits with code 0. Output shows 0 failed, N todo (skipped). No test crashes or import errors.</done>
</task>

</tasks>

<verification>
Full suite green after Wave 0:
```
cd c:/ClaudeDev/dictator && npx vitest run --reporter=verbose
```
Expected: exit code 0, all tests either "todo" (skipped) or passing, zero failures.
</verification>

<success_criteria>
- `vitest.config.ts` exists with jsdom + react plugin + setupFiles pointing to `src/test/setup.ts`
- `src/test/setup.ts` mocks MediaRecorder, WakeLock, getUserMedia globally
- All 10 stub test files exist with `it.todo` entries matching VALIDATION.md requirement mappings
- `npx vitest run` exits 0 with no failures
- `npm test` script is wired in package.json
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
