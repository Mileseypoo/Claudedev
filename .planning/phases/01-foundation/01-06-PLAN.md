---
phase: 01-foundation
plan: "06"
type: execute
wave: 3
depends_on:
  - "01-04"
  - "01-05"
files_modified:
  - src/app/(session)/active/page.tsx
  - src/app/(session)/recovery/page.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - SESS-01
  - SESS-02
  - SESS-03
  - SESS-05
  - SESS-06

must_haves:
  truths:
    - "Tapping Start Session on the home screen opens the consent modal, then immediately starts recording when confirmed"
    - "The active session screen shows a pulsing mic indicator, running timer, and Pause/End controls"
    - "WakeLock is requested on session start; keep-screen-on banner shows if WakeLock fails"
    - "Switching apps or locking screen pauses the session and shows the Recording Stopped banner"
    - "Pause and Resume work mid-session without losing the transcript"
    - "End requires double-tap confirmation; session is finalized and transcript persisted"
    - "Re-opening the app with an in-progress session shows the recovery screen with Resume/Start Fresh"
    - "Choosing Resume on the recovery screen reconnects to the existing session"
    - "Choosing Start Fresh on the recovery screen discards the previous session and clears the recovery key"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Home screen: ConsentModal wired to useSessionLifecycle.start(); navigates to /active on session start"
    - path: "src/app/(session)/active/page.tsx"
      provides: "Active session screen: all hooks and components wired together; full session lifecycle"
    - path: "src/app/(session)/recovery/page.tsx"
      provides: "Recovery screen: Resume (continues session) and Start Fresh (clears key, goes to /) wired to real handlers"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/hooks/useSessionLifecycle.ts"
      via: "ConsentModal onConfirm calls lifecycle.start()"
      pattern: "lifecycle\\.start|onConfirm"
    - from: "src/app/(session)/active/page.tsx"
      to: "src/hooks/useAudioCapture.ts"
      via: "startRecording() called after lifecycle transitions to active"
      pattern: "startRecording|useAudioCapture"
    - from: "src/app/(session)/active/page.tsx"
      to: "src/hooks/useDeepgramStream.ts"
      via: "startStream(sessionId, mimeType) called with sessionId from lifecycle and mimeType from audio capture"
      pattern: "startStream|useDeepgramStream"
    - from: "src/app/(session)/active/page.tsx"
      to: "src/hooks/useVisibilityGuard.ts"
      via: "onHide calls lifecycle.interrupt() + stream.pauseStream(); onShow updates banner"
      pattern: "useVisibilityGuard"
    - from: "src/app/(session)/recovery/page.tsx"
      to: "SESSION_RECOVERY_KEY"
      via: "Reads sessionId from localStorage; Resume navigates to /active with sessionId param"
      pattern: "SESSION_RECOVERY_KEY"
---

<objective>
Wire all hooks and components into the three page files to deliver the complete, working session flow. This is the integration plan — no new logic, only connections. After this plan, the full Phase 1 user journey works end-to-end (except device-level PWA install which is verified in Plan 07).

Purpose: Plans 03-05 built isolated pieces. This plan assembles them into a working application. SESS-06 crash recovery is only fully testable once the pages are wired.
Output: Three updated page files that compose all Phase 1 hooks and components into a working session lifecycle.
</objective>

<execution_context>
@C:/Users/Miles/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Miles/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-04-SUMMARY.md
@.planning/phases/01-foundation/01-05-SUMMARY.md
</context>

<interfaces>
<!-- Hooks and components created in Plans 04 and 05 -->

src/hooks/useAudioCapture.ts:
```typescript
export function useAudioCapture(): {
  requestMic(): Promise<MicResult>
  startRecording(onDataAvailable: (blob: Blob) => void): void
  pauseRecording(): void
  resumeRecording(): void
  stopRecording(): void
  mimeType: string
  isRecording: boolean
  isPaused: boolean
  error: MicError | null
}
```

src/hooks/useDeepgramStream.ts:
```typescript
export function useDeepgramStream(): {
  startStream(sessionId: string, mimeType: string, options: { onMicInterrupted: () => void }): void
  stopStream(): void
  pauseStream(): void  // sends KeepAlive
  sendAudioChunk(blob: Blob): void
  connectionState: 'idle' | 'connecting' | 'connected' | 'disconnected'
}
```

src/hooks/useSessionLifecycle.ts:
```typescript
export function useSessionLifecycle(): {
  status: SessionStatus
  sessionId: string | null
  elapsedSeconds: number
  start(consentTimestamp: string): Promise<void>
  pause(): Promise<void>
  resume(): Promise<void>
  end(): Promise<void>
  interrupt(): void
}
```

src/hooks/useWakeLock.ts:
```typescript
export function useWakeLock(): {
  wakeLockActive: boolean
  needsManualWarning: boolean
  requestWakeLock(): Promise<void>
  releaseWakeLock(): Promise<void>
}
```

src/hooks/useVisibilityGuard.ts:
```typescript
export function useVisibilityGuard(onHide: () => void, onShow: () => void): void
```

Components (all accept explicit props — see Plan 05 action section for signatures):
- ConsentModal: { isOpen, onConfirm }
- MicIndicator: { isRecording }
- SessionTimer: { elapsedSeconds }
- SessionControls: { onPause, onResume, onEnd, isPaused }
- StatusBanner: { showWakeLockWarning, showMicStopped, onResume }
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire home page and active session page</name>
  <files>
    src/app/page.tsx,
    src/app/(session)/active/page.tsx
  </files>
  <behavior>
    Home page (src/app/page.tsx):
    - When localStorage has SESSION_RECOVERY_KEY: router.replace('/recovery') (existing behavior from Plan 03, unchanged)
    - When no recovery key: renders start-session screen with ConsentModal
    - ConsentModal isOpen is controlled by a "Start Session" button tap
    - ConsentModal onConfirm: calls lifecycle.start(consentTimestamp), then on success router.push('/active')

    Active session page (src/app/(session)/active/page.tsx):
    - On mount: calls audio.requestMic() (NOTE: page mount IS triggered by user gesture — user tapped Confirm — so this is acceptable; but if requestMic was not already called in consent handler, call it here)
    - Actually: requestMic() should have been called in the consent onConfirm handler BEFORE navigating. The stream should be passed via context or the audio hook should expose the in-progress stream. Design: use a React Context `SessionAudioContext` to share stream/recorder between pages, OR (simpler for POC) call requestMic() in the active page's useEffect with the understanding the page was just navigated to from a user action.
    - SIMPLER APPROACH: In the active page, call audio.requestMic() inside a useEffect that runs once on mount. The navigation to /active was triggered by a user gesture (consent confirm), so some browsers will allow getUserMedia in the subsequent page load. If this fails on iOS, add a "Tap to begin recording" fallback button that calls requestMic() directly as a gesture handler.
    - On successful mic: call audio.startRecording(blob => stream.sendAudioChunk(blob))
    - Call stream.startStream(lifecycle.sessionId!, audio.mimeType, { onMicInterrupted: () => { lifecycle.interrupt(); } })
    - Call wakeLock.requestWakeLock()
    - Render full dark screen layout:
      - Top: `{/* Phase 3: cards will appear here */}` spacer
      - Center: `<MicIndicator isRecording={audio.isRecording} />` + `<SessionTimer elapsedSeconds={lifecycle.elapsedSeconds} />`
      - Top banner: `<StatusBanner showWakeLockWarning={wakeLock.needsManualWarning} showMicStopped={lifecycle.status === 'interrupted'} onResume={handleResume} />`
      - Bottom: `<SessionControls onPause={handlePause} onResume={handleResume} onEnd={handleEnd} isPaused={lifecycle.status === 'paused'} />`
    - handlePause: `lifecycle.pause()` + `audio.pauseRecording()` + `stream.pauseStream()`
    - handleResume: `lifecycle.resume()` + `audio.resumeRecording()` (stream resumes because KeepAlive kept it open)
    - handleEnd: `lifecycle.end()` + `audio.stopRecording()` + `stream.stopStream()` + `wakeLock.releaseWakeLock()` → then `router.push('/')` (return to home)
    - useVisibilityGuard: onHide → `lifecycle.interrupt(); audio.pauseRecording(); stream.pauseStream()` | onShow → (no auto-resume — banner stays until agent taps Resume per locked decision)
  </behavior>
  <action>
    Replace the placeholder content in `src/app/page.tsx` (created in Plan 03) and `src/app/(session)/active/page.tsx` with fully wired implementations per the behavior block.

    For the "Start Session" button on the home page: the tap opens ConsentModal (sets `consentModalOpen = true`). The ConsentModal's onConfirm calls `lifecycle.start(timestamp)` — which POSTs to /api/session/start and writes localStorage. On success, `router.push('/active')`.

    For the active page requestMic() timing concern: implement the "Tap to begin recording" fallback. Show a brief "Tap to start audio" overlay if mic access hasn't been granted yet. This handles the iOS Safari user-gesture requirement robustly without relying on navigation-gesture inheritance.

    Use `useRouter` from `next/navigation` for all navigation. The active page is `'use client'`.
  </action>
  <verify>
    <automated>cd c:/ClaudeDev/dictator && npx vitest run src/app/page.test.tsx --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>
    Home page: ConsentModal wired; lifecycle.start() called on confirm; router.push('/active') on success.
    Active session page: all hooks composed; MicIndicator, SessionTimer, SessionControls, StatusBanner rendered; visibilitychange handled.
    `npx vitest run src/app/page.test.tsx` exits 0.
    `npx tsc --noEmit` clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire recovery page</name>
  <files>src/app/(session)/recovery/page.tsx</files>
  <behavior>
    - Recovery page reads SESSION_RECOVERY_KEY from localStorage on mount
    - Shows sessionId and startedAt in the UI (e.g., "Session started at [time]")
    - "Resume session" button: navigates to /active with sessionId as query param; does NOT call lifecycle.start() (session already exists in DB)
    - "Start fresh" button: calls POST /api/session/end for the old sessionId (marks it ended), clears SESSION_RECOVERY_KEY, navigates to /
    - If SESSION_RECOVERY_KEY is missing or malformed: immediately redirect to /
    - If session data is partially corrupted (JSON parse fails): show warning "Some of this session may be missing" above the Resume button, but still offer Resume
  </behavior>
  <action>
    Replace the placeholder content in `src/app/(session)/recovery/page.tsx` with the fully wired implementation.

    Read `SESSION_RECOVERY_KEY` from localStorage using `useEffect` (client-side only). Parse the `SessionRecoveryState` JSON.

    "Resume session" flow:
    - `router.push('/active?sessionId=' + recoveryState.sessionId)`
    - The active page needs to detect the `?sessionId` param and skip the consent flow + session start API call, using the existing session ID instead. Update `active/page.tsx` to accept an optional `sessionId` searchParam — if present, skip lifecycle.start() and set the sessionId directly in the lifecycle hook. Add a `restore(sessionId: string)` method to `useSessionLifecycle` that sets `sessionId` and transitions to 'active' without POSTing to /api/session/start.

    "Start fresh" flow:
    - If old sessionId exists in recovery state: fire `fetch('/api/session/end', { method: 'POST', body: JSON.stringify({ sessionId: old.sessionId, durationSeconds: 0 }) })` — best effort, don't await or block on it
    - `localStorage.removeItem(SESSION_RECOVERY_KEY)`
    - `router.push('/')`
  </action>
  <verify>
    <automated>cd c:/ClaudeDev/dictator && npx vitest run src/app/page.test.tsx --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>
    Recovery page reads localStorage, shows session info, Resume navigates to /active with sessionId, Start Fresh clears key and navigates to /. `npx vitest run src/app/page.test.tsx` exits 0 (includes recovery screen test cases). `npx tsc --noEmit` clean.
  </done>
</task>

</tasks>

<verification>
Full Phase 1 automated suite:
```
cd c:/ClaudeDev/dictator && npx vitest run --reporter=verbose
```
Expected: All implemented tests pass. No failures. Exit 0.

Build check:
```
cd c:/ClaudeDev/dictator && npx next build
```
Expected: Build completes without TypeScript errors.
</verification>

<success_criteria>
- Home page: "Start Session" → ConsentModal → consent confirms → lifecycle.start() → navigate to /active
- Active session page: all hooks composed correctly; visibilitychange pauses session; no auto-resume
- Recovery page: Resume navigates to /active with existing sessionId; Start Fresh clears state and goes to /
- `useSessionLifecycle` has `restore(sessionId)` method for recovery resume path
- All page.test.tsx recovery screen tests pass
- `npx next build` succeeds
- All 16 VALIDATION.md requirement tests are either green or properly stubbed as todo
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-06-SUMMARY.md`
</output>
