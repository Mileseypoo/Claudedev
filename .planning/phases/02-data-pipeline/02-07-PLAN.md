---
phase: 02-data-pipeline
plan: 07
type: execute
wave: 4
depends_on:
  - 02-06
files_modified:
  - .env.local
  - vercel.json
autonomous: false
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - DATA-06

must_haves:
  truths:
    - "All Phase 2 automated tests pass on the deployed build"
    - "Admin can upload a real CSV and see status turn to Indexed within 60 seconds"
    - "Admin can upload a real PDF and see status turn to Indexed within 5 minutes"
    - "Admin can delete an upload from the portal and it disappears from the list"
    - "Stats preview shows accurate listing counts from the uploaded CSV"
    - "Query against listings table returns structured results (not RAG)"
  artifacts:
    - path: ".env.local"
      provides: "OPENAI_API_KEY env var added for embedding generation"
      contains: "OPENAI_API_KEY"
    - path: "vercel.json"
      provides: "Fluid Compute config if needed for waitUntil background tasks"
  key_links:
    - from: "Admin portal (/admin)"
      to: "Supabase database"
      via: "uploads table, listings table, document_chunks table"
      pattern: "status.*indexed"
    - from: "Vercel deployment"
      to: "@vercel/functions waitUntil"
      via: "Fluid Compute background processing"
      pattern: "waitUntil"

user_setup:
  - service: supabase
    why: "Phase 2 DB migration must be applied before any uploads will work"
    dashboard_config:
      - task: "Apply db/migrations/002_data_pipeline.sql"
        location: "Supabase Dashboard > SQL Editor — paste and run the migration"
      - task: "Enable pgvector extension"
        location: "Supabase Dashboard > Database > Extensions > search 'vector' > enable"
      - task: "Create storage bucket named 'uploads'"
        location: "Supabase Dashboard > Storage > New bucket > name: uploads, private: true"
  - service: openai
    why: "PDF embedding generation requires OpenAI API key"
    env_vars:
      - name: OPENAI_API_KEY
        source: "OpenAI Platform > API Keys > Create new secret key"
---

<objective>
Integration wiring: apply the DB migration, configure env vars, deploy to Vercel, and physically verify both upload pipelines work end-to-end.

Purpose: Automated tests cover logic in isolation; this plan verifies the complete system under real conditions — real Supabase, real OpenAI embeddings, real Vercel background processing. Requires human checkpoint to confirm status badges, timing, and PDF processing complete within 5 minutes.
Output: Phase 2 live on Vercel with confirmed working upload pipelines.
</objective>

<execution_context>
@C:/Users/Miles/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Miles/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-data-pipeline/02-CONTEXT.md
@db/migrations/002_data_pipeline.sql
@.planning/phases/02-data-pipeline/02-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final test sweep, env var check, and Vercel deploy</name>
  <files>.env.local, vercel.json</files>
  <action>
1. Run full test suite and confirm all tests pass:
   `npx vitest run --reporter=verbose`
   Fix any failures before proceeding.

2. Verify OPENAI_API_KEY is set in .env.local. If missing, add placeholder comment:
   ```
   OPENAI_API_KEY=sk-... # Required for PDF embedding generation
   ```
   Note: The actual key must be added by the user (see user_setup in frontmatter).

3. Check if vercel.json exists. If not, create it with:
   ```json
   {
     "functions": {
       "src/app/api/admin/upload/route.ts": {
         "maxDuration": 300
       }
     }
   }
   ```
   This sets the upload route timeout to 300 seconds (Vercel Hobby max) to accommodate large PDF processing via waitUntil.

4. Add OPENAI_API_KEY to Vercel environment variables:
   `npx vercel env add OPENAI_API_KEY production` (if vercel CLI available)
   Otherwise note in output that user must add this via Vercel dashboard.

5. Deploy to Vercel:
   `npx vercel --prod`
   Capture the deployment URL.
  </action>
  <verify>
    <automated>cd C:/ClaudeDev/dictator && npx vitest run --reporter=verbose 2>&1 | tail -10</automated>
  </verify>
  <done>Full test suite passes (0 failures). vercel.json exists with upload route timeout. Deployment URL captured.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: User setup — apply DB migration and configure services</name>
  <action>Human must complete the following before verification can proceed (Claude cannot access external dashboards).</action>
  <what-built>
    The following must be completed before the verification checkpoint:
    1. OPENAI_API_KEY added to .env.local and Vercel environment variables
    2. db/migrations/002_data_pipeline.sql applied via Supabase SQL Editor
    3. pgvector extension enabled in Supabase Dashboard > Database > Extensions
    4. 'uploads' storage bucket created (private) in Supabase Dashboard > Storage
  </what-built>
  <how-to-verify>
    After completing the setup steps:
    - Visit Supabase Dashboard > Table Editor — verify tables: uploads, listings, document_chunks, listing_stats all exist
    - Visit Supabase Dashboard > Storage — verify 'uploads' bucket exists
    - Visit Supabase Dashboard > Database > Extensions — verify 'vector' extension is enabled
  </how-to-verify>
  <resume-signal>Type "setup complete" when all 4 setup steps are done and verified in Supabase dashboard</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: End-to-end verification — CSV and PDF upload pipelines</name>
  <action>Human verifies the live deployment. Claude has already run tests and deployed to Vercel in Task 1.</action>
  <what-built>
    Full Phase 2 data pipeline deployed to Vercel:
    - /admin portal with file upload zone, uploads list with status badges, stats preview
    - POST /api/admin/upload handles CSV (sync, 201) and PDF (async, 202 + waitUntil)
    - GET /api/admin/uploads returns live upload list (5-second polling)
    - DELETE /api/admin/uploads/[id] removes upload + storage file
    - GET /api/admin/stats returns pre-calculated listing stats
  </what-built>
  <how-to-verify>
    1. Open [deployment-url]/admin in a desktop browser
    2. Download the CSV template: click "Download template" link — should download a valid CSV with header row
    3. Fill in 3-5 sample rows in the template CSV and save
    4. Upload the CSV via the upload zone — status badge should show "Indexed" within 10 seconds
    5. Check stats preview — should show "X listings indexed" matching your CSV row count
    6. Upload a small PDF (any PDF, under 50MB) — status badge should show "Processing" immediately, then transition to "Indexed" within 5 minutes
    7. While PDF is "Processing", wait 5 seconds — badge should update without page refresh (confirms polling)
    8. Delete the CSV upload — row disappears from the list
    9. Verify in Supabase Dashboard > Table Editor > listings — rows from your CSV upload are present
    10. Verify in Supabase Dashboard > Table Editor > document_chunks — rows from your PDF upload are present with embedding values
  </how-to-verify>
  <resume-signal>Type "verified" if all 10 steps pass, or describe any failures so they can be investigated</resume-signal>
</task>

</tasks>

<verification>
Full Phase 2 verification:
1. `npx vitest run` — all tests green (63 Phase 1 + all Phase 2 additions)
2. `npx tsc --noEmit` — no TypeScript errors
3. /admin page loads at deployment URL
4. CSV upload flow confirmed end-to-end by human checkpoint
5. PDF upload + background processing confirmed by human checkpoint
6. Stats display confirmed accurate
7. Delete flow confirmed working
</verification>

<success_criteria>
- All automated tests pass (0 failures)
- /admin portal accessible at deployment URL
- CSV upload: status Indexed within 60 seconds, listings appear in DB, stats update
- PDF upload: returns 202 immediately, status transitions to Indexed within 5 minutes, document_chunks populated with embeddings
- Delete: removes upload from list, clears storage file and DB record
- Template CSV downloads correctly
- Human checkpoint signed off
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-pipeline/02-07-SUMMARY.md`
</output>
